We all are known to the “Bucket” tool of Microsoft Paint which is used to fill an area with single specific color. But do we know how it actually
works? Well, Let’s discuss this.

We are going to discuss the algorithm behind the bucket tool. And this particular algorithm is known as Flood Fill Algorithm.

for more in detail study kindly refer to article on hackernoon. 